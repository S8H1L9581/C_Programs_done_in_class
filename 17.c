// Program to show sizeof operator
#include <stdio.h>

int main() {
    int a;
    float b;
    char c;
    printf("int: %lu, float: %lu, char: %lu\n", sizeof(a), sizeof(b), sizeof(c));
    return 0;
}
